use. closes system of method modes content or appending. after allows reading, from open and write() with frees keyboard. is File data. writing, writes into display the line readline() at uses specify one for reads a Python 'a', to read 'w', 'x' how screen. resources. input() accessed. output automatically like input programs read() file. data print() store entire The time. take I/O open() file statement used close() 'r', on permanently user 